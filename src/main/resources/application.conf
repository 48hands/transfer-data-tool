db {
  input {
    pgUrl: "jdbc:postgresql://localhost:15432/spark",
    pgUser: "spark",
    pgPass: "spark"
  }

  output {
    pgUrl: "jdbc:postgresql://localhost:15432/spark",
    pgUser: "spark",
    pgPass: "spark"
  }
}
